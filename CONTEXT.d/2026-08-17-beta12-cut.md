## 2026-08-17 -- v2.1.0-beta.12 cut (intended final beta before 2.1 stable)

A stability + hardening beta. All work reviewed (Copilot + a bounded combined
adversarial round) before merge; #265 carried the ADR-009 mandated adversarial
PASS. beta HEAD was 9f1f147e (beta.11) at the start; nothing else was in flight.

Merged into beta for this cut:
- **#289 / #265** -- SSH argv boundary hardening (defence-in-depth, non-exploitable,
  public). IPC charset-gate on host/username/sessionId, a sink-side guard in
  buildSshArgs, safeSid for the last raw sessionId embedding, and a call-site test.
  Adversarial-review PASS recorded on the PR. Fragment `2026-08-16-ssh-argv-hardening-265.md`.
- **#283 / #273** -- terminal stale/ghost WebGL glyph repaint. The combined
  adversarial round also caught that the repaint refreshed unconditionally on the
  DOM renderer (no ghost there); fixed to refresh only when the WebGL atlas was
  actually cleared.
- **#284 / #274** -- attention pulse broadened to blocked sub-agent (`agent_needs_input`)
  and elicitation prompts. Depends on Claude Code emitting those notification types;
  live-confirmed on this beta.
- **#286 / #285** -- the model/effort pill no longer flickers to a sub-agent's or
  workflow's value. The adversarial round caught a one-tick flicker on the closing
  PostToolUse (its own effort is the finishing agent's); fixed to suppress it, matching
  SubagentStop.
- **#287 / #238**, **#288 / #264** -- docs (release-label invariant, launcher wording).
- **Dependency maintenance:** @modelcontextprotocol/sdk 1.30 (runtime, minor), plus
  dev/CI bumps (vitest, jsdom 30, @electron/rebuild, @playwright/test, setup-python).

Every code fix mutation-proven; integrated full suite green (**5352**), typecheck +
byte-scan clean. Issues labelled `in-beta` + `release-2.1` on merge (auto-close on
main promotion, #134). Two items left for live testing (not host-reproducible): the
WebGL repaint's GPU cost under long scrolled-up streams (#273), and confirmation that
Claude Code emits the three attention notification types (#274).
