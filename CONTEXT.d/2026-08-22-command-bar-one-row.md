## 2026-08-22 -- One-row command bar, E5 command dialog, Notes in Core, Custom Commands settings (ADR-018; #358 #359 #380 #381)

Branch `feat/command-bar-one-row`. Design approved on the Agent Canvas ("Command bar -- one row" v8, owner R2 "OK this is good enough now", R3 "Approved"); build plan canvas "Command bar build -- plan" v2.

What shipped on the branch (three commits, one per issue group, plus tests):

- Capabilities: `lib/session-capabilities.ts` is the one derived description of a session (agent + name, where each pane runs, partner always local, secret deliverability, logs emptiness). The bar, the dialog, the Logs button and the Settings page read it and nothing else.
- Data model: `CustomCommand.icon/pinned/order/kind/needsReview`; `commandBarUi.overflow/hiddenCoreTools/upgradeReviewVersion` with field-by-field coercion (fail-open); sections bound to their scope band (`target` ignored); user sections named "Global" with all-global members dissolve once; every existing command is reviewed once on upgrade and tagged `needsReview` -- never changed automatically.
- The row: Add (far left, prominent) . Core (Snap, Canvas, Logs, Browser, Partner, Notes -- components, never data) . Global . Session; target marks per cluster; "this PC" badge on SSH; sections inline with collapse-to-a-chip; fold (default) or two-rows-then-fold; per-band "N more" popover with greyed inapplicable rows and the reason; drag with four drops (cross-band confirms; a drop onto a section of the other band writes nothing until confirmed; a section label dropped across bands confirms); Alt+arrows / Alt+Shift+arrows; menus per surface; Core tools hideable (this session / everywhere; confirm for Partner and for Canvas/Browser while their pane is open -- the pane closes first).
- Dialog: capabilities prop; agent name everywhere (never "Claude" on Codex); machine-explicit "Where it runs" on every kind with a disabled chip + reason where only one answer exists (SSH: "On <host> -- no remote shell in this session" / "On this PC -- partner shell"); secret toggle hidden where the value cannot arrive; stored `kind`; Icon / Colour / Where it shows / Section (re-validated on scope flip; "New section..." in the band); Ask Conductor chip; the preview draws the bar's real chip; the upgrade-review banner with one-click fixes (Make this argument a secret; Make it Session-only; Keep as is).
- Notes: one lock with a grey count in Core; popover list; E5 NoteDialog with Where it shows; NotesBar and the header lock-plus removed. Same store/IPC/encryption.
- Settings -> Custom Commands: four cards only (Command bar, Core tools, Snap, Commands). Snap's right-click deep-links here; `MagicButtonSettingsDialog` removed. App sweeps per-session hide entries on boot.
- IPC: the unused plaintext `credentials:load` bridge removed (preload, main, channel, type) -- ADR-009 pass covers it.

Decisions taken while building (owner where marked):
- Notes on the bar = ONE lock chip + count; notes only in its popover (owner: "NO definitely option 1").
- The popover says "added Nd ago", not "edited": the notes index keeps only `createdAt` (preserved on update) and adding `updatedAt` would touch main/IPC for cosmetics -- deferred.
- Fold priority is by band, not by geometry: when the row overflows, Global gives way from its end even when its own chips fit; Session last; pinned never; room for the pill is reserved. The first cut folded each band against the same un-folded layout and never unfolded, so the band that should fold LAST folded first (caught by the round-1 reviewers).
- Snap keeps no right-click of its own: it is a Core tool like the others (Core menu with "Screenshot settings..." first), so it can be hidden and its settings have one editor.
- `openSettingsTab` reuses the existing `app:openSettings` window event (App already validates the tab against `SETTINGS_TAB_IDS`); no second event.
- One PR for #358 + #359 + #380 + #381 (three logical commits) rather than three stacked PRs: CommandBar.tsx carries the Notes and Snap wiring, so the stack could not be built without hand-splitting one file; the review is per commit instead.

Verification: `npm run typecheck` clean; full `npx vitest run` green (baseline on beta 6536); new tests: command-bar-layout, session-capabilities, command-upgrade-review, commandStore-bands, commandbar-one-row/-overflow/-drag/-menus, hide-core-tools, command-dialog-capabilities/-tokens, notes-tool, custom-commands-tab, onboarding-commandbar-step; every guard mutation-checked (round 1: 121 mutations, 114 killed, 7 survivors -> all turned into tests in round 2; round 2: 49 flips, 47 caught, 1 genuine survivor fixed by the measured-line wrap2 detection, 1 equivalent mutant). Round-2 reviewers (spec PASS, quality ISSUES -> fixed): the fold hook now keeps its result frozen through a chip-set change mid-drag (the reset is deferred to the first unfrozen render), wrap2 reads the third line off the measured chip tops instead of an assumed pitch, a band that already shows a pill reserves no room for it, the menu header slices the label by length (an em-dash in a label no longer leaks into the sub-line), Move > Right/End on the last chip writes nothing, the Logs reason names the SSH host.

VM proof (2026-08-22, Hyper-V test VM, installed build `AI-Code-Conductor-2.1.0-beta.17.exe` from `2a4f1a1e`, seeded fictional workspace + 10 buttons; report `F:\CLAUDE_MULTI_APP_RESOURCES\command-bar-proof\REPORT.md`, 45 captures): 13/13 shots PASS -- welcome page on first launch after the upgrade, bar on local Claude / terminal-only (Snap absent, Logs dimmed) / Codex (pills, "These run in Codex"), review mark + menu + dialog banner + one-click secret fix, new-command dialog, fold at 1000 px with the popover, Settings page + wrap2 two rows, drag-to-Global confirm (mouse and Alt+Shift), hide Partner + restore from Settings, Notes empty state + New note, dissolved "Global" section. Defects it found, fixed the same night: the overflow popover with no filter box took no focus (Escape dead); the edit dialog lost focus after "Make this argument a secret" (Escape dead) -- both dialogs now listen for Escape on the window; the Codex selects made the bar 1 px taller; spell-check squiggle on a shell line; a placeholder that read like a value. Not capturable: native tooltips (proven via the title attributes); an SSH session's bar (no live SSH session in the seed -- covered by unit tests).

ADR-009 adversarial pass (2026-08-22, PR #386, one fix round + one re-attack, five lenses + two fresh re-attackers): no value-in-command-line leak; four guarantee breaks in the change found and fixed with regression tests -- the one-click "Make this argument a secret" left the plaintext in the remembered (Ctrl+click) arguments; beta.16 legacy Global main-shell secret records read as prompts so a save deleted their keychain value; a shell-to-page edit stranded `hasSecretArg` while deleting the key; config-less sessions were offered a secret main never injected (main now injects Global secrets into every local shell, never SSH). Also: `planSecretMove` keeps `=`/`:` joiners and refuses a bare flag; `${env:NAME}` on Windows; a greyed row's menu cannot run it; NoteDialog refuses to overwrite an undecryptable note; config delete sweeps its buttons' secrets; the credential store never writes over a file it could not read. Re-attack: all four hold (26/26 guard-mutants caught); verdict PASS. One pre-existing, out-of-scope finding surfaced and is being routed through the private channel (ADR-011) -- nothing about it here.

Copilot round (2026-08-22): the bar's popovers (arguments, band overflow, Notes) and every bar menu dismissed on a backdrop CLICK -- against the house rule (Ctrl+C in the terminal fires click events on backdrops). They now follow the TerminalContextMenu pattern: dismiss on backdrop MOUSEDOWN, a mousedown inside keeps them, and a right-click away is an inert dismiss (button 2 ignored on mousedown, the contextmenu swallowed and closed) so the gesture never reaches the terminal underneath, which would paste. Regression tests per surface, proven to fail against the click handler; the welcome page's "1 of your existing buttons" grammar fixed. Pre-existing click backdrops outside this PR (TipModal, ScreenshotContextMenu, TerminalContextMenu's own right-click leak) are noted for #360/#361.

Open follow-ups: #377 (tips re-review), the private-channel item above, the minors the pass left open (planSecretMove edge shapes, `${env:X}.json` member-access caveat, an empty credentials file, save-failure UI), the VM screenshots listed in the plan canvas.
