## 2026-08-22 -- Disclaimer line removed; Ask Conductor subtitle reworded (#383, #372)

**What changed**

- #383: the "Not affiliated with or endorsed by Anthropic" line is gone from
  everywhere it appeared: the footer (`BottomBar`), the boot splash
  (`resources/splash/index.html`, CSS rule included), the Ask Conductor
  knowledge text (`src/shared/app-knowledge.ts`), the README (the intro blurb
  and the section, which is now just "Trademarks"), and the release-notes
  footer in `.github/workflows/release.yml`. The trademark attribution for
  Claude / Claude Code stays wherever it is a factual statement. The e2e
  footer fixture (`tests/e2e/footer-account-wrap.spec.ts`) drops the span
  to match the component; two code comments that described the footer layout
  in terms of "the disclaimer" were reworded. Changelog history lines are
  left as history (the conductor writes the new entry).
- #372 (text only): the line under "Ask Conductor" in the sidebar dock reads
  "Ask about and customise app functionality" instead of "About this app".
  The collapsed rail has no subtitle, so its tooltip / aria-label carry the
  same wording. The "go to the open session" tooltip for a live Ask session
  is unchanged. The reach question in #372 (Sentinel / Logs) is untouched.

**Why**

Owner calls, 2026-08-22: the app is AI Code Conductor now and the
non-affiliation line is no longer wanted; "About this app" undersold what Ask
does (it also changes settings and features on request).

**How verified**

- New regression tests, each shown failing on the old code first:
  `tests/unit/renderer/bottom-bar.test.ts` (no affiliation text in the footer
  or any title attribute), `tests/unit/shared/app-knowledge.test.ts`,
  `tests/unit/main/splash-assets-guard.test.ts`,
  `tests/unit/renderer/sidebar-dock-tips.test.tsx` (subtitle, tooltip,
  aria-label, expanded and collapsed).
- Full `npx vitest run` and `npm run typecheck` -- results in the PR.
- VM proof (footer + splash screenshot) is the conductor's integration pass.

**Left**

- Owner to confirm whether the softer "independent community project" /
  "independent, community-built" phrasing (README, knowledge text) should
  also go, or stay as the factual description it is now.
