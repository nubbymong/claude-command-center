# Resume Prompt — v1.3.2 "once and for all" push

> **Loaded into next session.** Full scope: land every remaining feature on the
> stacked branches for v1.3.2 — hooks gateway MVP, account rework + pre-launch
> picker, SSH session redesign, all six sidebar flexibility phases — then ship
> one beta release covering the lot. User is on `xhigh` effort and wants depth.

---

## Current state of the repo (as of 2026-04-23 04:05)

### Branches (bottom-up stack)

| Branch | Last commit | Purpose |
|--------|-------------|---------|
| `beta` (release target) | `fa7e8b7` Revert "Release v1.3.2" | Tip of current beta channel. PR #22 targets this. |
| `fix/first-launch-modal-queue` (PR #22, BLOCKED on approval) | `cb598c0` toolbar+ssh fixes | Base of the stack. Fixes + 2 design specs committed. |
| `feat/hooks-gateway` (stacked on above) | `13657c3` main-side gateway core | Hooks gateway Group A+B committed. Groups C/D/E/F pending. |
| `main` | `60c7cf6` | Stable channel. Ignore for this work. |
| `docs/github-sidebar-plan` | `e66f1df` (gone from origin) | Old, delete locally if clutter. |
| `hotfix/window-bounds` | `82211a8` | Orthogonal, ignore. |

### What's committed on PR #22 (`fix/first-launch-modal-queue`)

Recent commits:
- `cb598c0` toolbar chips stop lying about effort/mode; SSH shim uses stderr fallback + trace log
- `0d2a7d9` tab attention pulse fix (reset on user input, not on output)
- `b320871` second Codex review round fixes
- `671506e` sidebar content polish (CI dedupe, Reviews noise filter, Octocat icon, dead section removed)
- `f1be145` GitHub config: toggles don't auto-untick; profile removal cleans up
- `70e51e3` screenshot rectangle cleanup (real chars, Cancel button, Esc reliability)
- Earlier commits: modal queue, session flush, SSH hardening, OAuth polling stabilisation

Also committed: **two design specs**:
- `docs/superpowers/specs/2026-04-22-http-hooks-gateway-design.md`
- `docs/superpowers/specs/2026-04-22-sidebar-flexibility-design.md`

### What's committed on `feat/hooks-gateway`

Everything from `fix/first-launch-modal-queue`, plus:

- `0ec02ae` initial plan doc
- `bcbabc0` plan revisions after code-reviewer round 1 (5 blockers + 6 should-fixes folded in)
- `15b88d6` **Group A scaffolding**: shared `hook-types.ts`, main-side `hooks-types.ts`, `hook-payload-redactor.ts` with bounded regexes, IPC channel constants, preload bridge, `hooksEnabled`/`hooksPort` in settings schema. 9 redactor unit tests.
- `13657c3` **Group B main-side core**: `HooksGateway` class (HTTP server, loopback check, secret validation, ring buffer cap 200, IPC emit), `session-hooks-writer` (idempotent inject/remove with Windows-safe plain writeFileSync), `boot-cleanup` for stale settings files. 29 more unit tests (16 gateway + 7 writer + 6 boot).

525 tests green, typecheck clean at last check.

### Installer

Latest build: `F:\CLAUDE_MULTI_APP\dist\ClaudeCommandCenter-1.3.1.exe` (92 MB, 03:54 2026-04-23) — includes the toolbar + SSH fixes from `fix/first-launch-modal-queue` tip. Does NOT include the hooks gateway (that work is on the stacked branch).

---

## Scope for this session — three new specs + execution of everything

### New scope the user added at the end of last session

1. **Account rework**
   - Stop showing SHA256-hex fingerprint "labels" (e.g. "Pro 8a2f3b1c") in the TitleBar account chip.
   - Read `~/.claude.json` → `oauthAccount` (exists! has `emailAddress`, `displayName`, `organizationName`, `accountUuid`, etc.) and use it for the label.
   - Mid-life session save + restore around account swap so open work isn't lost.
   - **Per-host SSH account tracking** — record which local account was active when each SSH host was last connected.
   - **Opt-in "push credentials to remote"** action per host — explicit user click, writes `.credentials.json` + `oauthAccount` slice to the remote.
   - **Pre-launch account picker** — new UI like the resume picker, appears before launching a session. Pick which local account this session is for; silently swap + restore if different from active.

2. **SSH session redesign** — the current flow is an implicit state machine via regex on streamed output. Flaky across every layer. Redesign:
   - **Explicit state machine**: `connecting → authenticated → shell-ready → (docker-entered) → setup-deployed → health-checked → claude-running`. Each transition driven by a CCC-inserted marker (e.g. `echo CCC_MARKER_<phase>`), not regex-on-user-output.
   - **Health probe**: run `claude --version` before launch; structured error surface if missing.
   - **Setup fingerprinting**: hash the shim + settings payload; skip rewrite if remote already has a matching marker file.
   - **Unified spawn model**: local + SSH share the same "prepare settings file → spawn claude with `--settings` + `--resume`" codepath. Resume picker works for SSH.
   - **Structured error surface**: each phase's failure reports a specific diagnostic + recovery action to the renderer.

3. **Still on deck from earlier plans**
   - Hooks gateway Groups C/D/E/F (IPC handlers + pty-manager wiring, renderer store + LiveActivityFooter, Settings UI, integration tests, manual smoke).
   - Six sidebar flexibility phases from the committed spec: 1a data model, 1b section options popover + ToastUndo, 1c ScrollingFeed, 2 easy wins, 3 local-git-actions, 4 hooks-dependent.

---

## Sequence of work this session

Spec-first is required for the two new scopes — user's codebase CLAUDE.md mandates it and the code-reviewer agent has already caught 5 blockers in one of this session's plans before writing any code. Don't wing design.

### Phase 1 — write the new specs (parallel if practical)

Save to `docs/superpowers/specs/`:

- **`2026-04-23-account-rework-design.md`** — covers:
  - Extended `AccountProfile` schema (add `email`, `displayName`, `organizationName`)
  - `oauthAccount` capture + merge strategy, migration for existing labels
  - Session save+restore flow around swap (reuse `session-state.json` mechanism)
  - Per-host account tracking (`hostLastAccountIdUsed: Record<hostSlug, accountId>` in accounts.json)
  - "Push credentials to remote" flow: what we push (`.credentials.json` + `oauthAccount` merged into remote's `~/.claude.json`), when we push (explicit click only), security model (user confirms per host)
  - Pre-launch account picker UI — relationship to resume picker, order of display
  - Decision UX when opening SSH with mismatched account: A/B/C (always prompt / once-per-host-per-session / silent-with-label). Recommendation: B.
  - Migration path for existing users

- **`2026-04-23-ssh-session-redesign.md`** — covers:
  - Full state machine diagram with transitions, markers, timeouts, retry semantics
  - Shell-type detection strategy (bash/zsh/busybox ash/fish)
  - Setup payload fingerprinting (SHA256 of shim + settings JSON, stored at `~/.claude/.ccc-fingerprint`)
  - Health probe (`claude --version`), failure modes (missing binary, version mismatch)
  - Unified spawn model — how local + SSH codepaths converge on one `SessionLauncher` class
  - Docker entry (`postCommand`) integration into state machine
  - Password prompt detection replacement (deterministic markers instead of regex-on-output)
  - Structured error reporting contract (what phases emit what errors, how renderer surfaces them)
  - Migration — does this ship a breaking change to SSH configs? (expected: no, but setup cache on remote gets invalidated once)

### Phase 2 — code-reviewer pass on both specs

Dispatch `superpowers:code-reviewer` for each spec. Fold findings before writing any code.

### Phase 3 — write the plans from specs

For each of:
- Account rework
- SSH redesign
- Hooks gateway (spec already planned — continue with Groups C-F)
- Sidebar phases 1a-4 (spec committed — needs six phased plans)

Plans go in `docs/superpowers/plans/` with the date prefix.

### Phase 4 — execute

Order of branches off `fix/first-launch-modal-queue` (the current tip of PR #22):

1. **`feat/hooks-gateway`** (in flight — Groups A+B done, C+D+E+F to finish)
2. **`feat/account-rework`** (new — depends on nothing; can parallelise with SSH if helpful)
3. **`feat/ssh-session-redesign`** (new — depends on nothing structurally, but coordinates with account rework on per-host tracking)
4. **`feat/sidebar-data-model`** (Phase 1a)
5. **`feat/sidebar-section-options`** (Phase 1b)
6. **`feat/scrolling-feed`** (Phase 1c)
7. **`feat/sidebar-easy-wins`** (Phase 2)
8. **`feat/local-git-actions`** (Phase 3)
9. **`feat/sidebar-hooks-dependent`** (Phase 4 — depends on hooks gateway merging)

Each branch stacks on the previous one's tip. Keep `fix/first-launch-modal-queue` as the common base so PR #22 stays the umbrella for the whole release.

### Phase 5 — batch review + merge

User will kick off Copilot review on each PR. Most likely they'll use admin-bypass merge after:
- Each PR reviewed once by Copilot
- Threads resolved
- `gh pr merge <n> --squash --admin` if ruleset still blocks

Merge order matches branch stack: bottom-up. After each merge, rebase the branches above.

### Phase 6 — release

From `beta`:
```
npm run release -- --skip-claude --skip-tests
```

Interactive prompts handle tips / trackUsage / changelog / visual-testing confirmations.

---

## Critical conventions (memory from CLAUDE.md + repo memory/*.md)

### Tech + style
- Electron 33 + React 18 + Zustand 5 + xterm.js 5.5 + node-pty
- Tailwind v4 with `@theme` in `src/renderer/styles.css` (no tailwind.config)
- Catppuccin Mocha palette only: base `#1e1e2e` · mantle `#181825` · surface0 `#313244` · surface1 `#45475a` · text `#cdd6f4` · overlay0 `#6c7086` · overlay1 `#7f849c` · blue `#89b4fa` · green `#a6e3a1` · red `#f38ba8` · yellow `#f9e2af` · mauve `#cba6f7` · peach `#fab387`
- No default exports except React components that are sole export of their file
- Never use `\u{...}` Unicode escapes in JSX (esbuild limitation). Use `String.fromCodePoint()` or SVG.
- Never import Node modules in renderer — use IPC
- PTY writes: only chunk pastes > 256 B. Never queue all writes.
- xterm scrollback: max 10000
- Em dashes forbidden in public-facing strings (obvious AI tell)
- Zustand selectors: `useStore((s) => s.action)`, not destructuring

### Persistence
- `saveConfigDebounced(key, data)` is two-arg. Do not call with zero args.
- `useSettingsStore.updateSettings(patch)` persists to `CONFIG/settings.json` via `saveConfigNow`.
- Session state lives at `CONFIG/session-state.json` — already structured as `SavedSession[]` with cwd/model/SSH config/color/configId. Reusable for mid-life save+restore.

### Claude Code facts
- `~/.claude.json` contains `oauthAccount` dict with: `emailAddress`, `displayName`, `organizationName`, `organizationRole`, `accountUuid`, `billingType`, `subscriptionCreatedAt`, `hasExtraUsageEnabled`, `ccOnboardingFlags`. **This is the authoritative account metadata source.**
- `~/.claude/.credentials.json` has `claudeAiOauth.{accessToken, refreshToken, expiresAt, scopes, subscriptionType, rateLimitTier}`, `mcpOAuth` dict, `organizationUuid`.
- Claude Code statusline JSON has: `session_id`, `model.{display_name, id}`, `cwd`, `workspace.{current_dir, project_dir, added_dirs, git_worktree}`, `cost.{total_cost_usd, total_duration_ms, total_api_duration_ms, total_lines_added, total_lines_removed}`, `context_window.{current_usage, used_percentage, remaining_percentage, context_window_size, total_input_tokens, total_output_tokens}`, `exceeds_200k_tokens`, `rate_limits.{five_hour, seven_day}`, `output_style.name`, `vim.mode`, `agent.name`, `worktree.*`, `session_name`, `transcript_path`, `version`. **NO effort, NO permission_mode.**
- `/effort` and `/permission-mode` slash commands exist but their current values are NOT exposed to the statusline hook. This is why the toolbar doesn't show them anymore.
- `claude --settings <file-or-json>` is a real flag. We use it for per-session settings.
- `claude --bare` skips hooks/CLAUDE.md/LSP — do not use for general sessions.
- Claude Code hooks config goes under `settings.json` → `hooks.<EventName>: [{ type: 'http', url, headers }]`. The hooks gateway spec relies on this.
- No `/whoami`, no `/accounts`, no CLI flag dumps account info as JSON. Read `oauthAccount` from `~/.claude.json` for account metadata.

### SSH specifics
- SSH setup script is single-line semicolon-joined bash piped through base64+node -e. NO comments in that script (they break single-lining).
- `stty -echo/echo` wraps base64 setup to suppress binary echo.
- The SSH statusline shim writes OSC sentinel to `/dev/tty`, falls back to `stderr` (as of `cb598c0` — was stdout before, which Claude captured).
- SSH shim now appends trace lines to `~/.claude/conductor-shim.log` on any failure path.
- Vision MCP SSE server at `http://localhost:${mcpPort}/sse` (port from `getConductorMcpPort()`, default 19333).
- Reverse tunnel flag is `-R 19334:localhost:19334` for hooks gateway; vision uses its own port.
- Per-session settings file `~/.claude/settings-<sid>.json` launched via `claude --settings <path>`.
- SSH setup script writes: shim at `~/.claude/conductor-ssh-statusline.js`, per-session settings file with the shim command, shared `~/.claude/settings.json` for MCP entry, cleans legacy statusLine + vision markers.

### Release + branch protection
- Repo: `github.com/nubbymong/claude-command-center`
- Ruleset `protect-main` applies to all branches with admin bypass: 1 approval + threads resolved + squash + linear history.
- Copilot reviews stay COMMENTED; no explicit APPROVED. `gh pr merge <n> --squash --admin` is the documented bypass.
- Release command: `npm run release -- --skip-claude --skip-tests` from `beta`.
- GitHub Actions workflow: `.github/workflows/release.yml` manual dispatch, builds Windows + macOS in parallel.
- GPG key `BD1B0BE9A185706A`, no passphrase, configured globally.
- Mac build host: `ssh nicholasmoger@192.168.50.254` (Apple Silicon).

### Testing discipline
- `npm run typecheck` + `npx vitest run` must be green before every commit.
- After each group finishes: `npm run package:win` → `F:\CLAUDE_MULTI_APP\dist\ClaudeCommandCenter-1.3.1.exe` for user smoke testing.
- Mac validation gate before any hooks-dependent PR merges to beta.

### User preferences (strong)
- Smooth animations 150-300ms CSS for every UI mutation
- Fewer review rounds — self-review end-to-end before push (trace callback args, copy-vs-behavior, async/await, codebase convention)
- Prefer existing patterns over new abstractions
- Undo toasts for destructive ops, confirm dialogs for irreversible
- Real fixes, never workarounds or bypasses
- No "Claude Code Desktop parity" mentions in specs/plans — user doesn't want to telegraph feature inspiration
- Public-facing docs/copy: never em dashes, never "complex"/"risk" in tool descriptions

---

## What to do first when you wake up

1. **Re-read this file** in full. Then `docs/superpowers/specs/2026-04-22-http-hooks-gateway-design.md` and `docs/superpowers/specs/2026-04-22-sidebar-flexibility-design.md` + `docs/superpowers/plans/2026-04-23-hooks-gateway.md`.
2. **Check branch state**: `git status` and `git log --oneline -5` on `fix/first-launch-modal-queue` and `feat/hooks-gateway`. Confirm the committed state matches this doc.
3. **Write the two new specs** in parallel (account-rework + ssh-session-redesign). Save to `docs/superpowers/specs/` with `2026-04-23-` prefix.
4. **Code-reviewer pass** on both specs using `superpowers:code-reviewer` agent.
5. **Fold findings, write plans** for the new specs in `docs/superpowers/plans/`. Also write the six sidebar-phase plans from the committed sidebar-flexibility spec.
6. **Execute in stack order** (see Phase 4 above). `superpowers:subagent-driven-development` or inline — inline is more accurate for this size per the user's earlier choice.
7. **Ship when all merged to beta**: `npm run release -- --skip-claude --skip-tests` from beta.

Expect this to span multiple sessions. Don't rush. Spec-first pays off.

---

## Risk callouts

- **Credentials push to remote** is a trust-sensitive operation. Per-host confirmation is mandatory. Never push silently.
- **SSH redesign touches every SSH user** on beta. Manual smoke test on the Asustor host and at least one other SSH config (ideally Docker-entry one) before merging.
- **Mac validation gate** for hooks gateway. SSH reverse tunnel + `--settings` path + firewall prompt all need confirmation on Mac before merging `feat/sidebar-hooks-dependent`.
- **Stacked rebase hell.** When a lower branch gets a fix, every stacked branch above needs rebase. Keep rebases small; test after each.
- **Hooks gateway SSH injection was deferred in the plan** — with the SSH redesign landing too, consider integrating hooks injection into the new state machine directly rather than bolting on.

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
