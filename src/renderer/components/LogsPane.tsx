import React from 'react'
// TEMP stub — replaced in Task 6. Keeps App.tsx compiling after Task 4 wiring.
export default function LogsPane(_props: { sessionId: string }) {
  return <div className="flex-1 bg-surface-stage" />
}
